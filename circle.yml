dependencies:
  pre:
    - sudo apt-get install python-mock
    - sudo apt-get install python-twisted
    - cd scripts && bash ./setup.sh -circleCI

test:
  override:
    - trial tests/test_receiveMsg
    - trial tests/test_connectionDown
    - trial tests/test_loginProcess
    - trial tests/test_sendMsg