sudo: false

language: 'python'

branches:
  only:
    - jrpc_if

python:
  - '2.7'

git:
  submodules: false

install:
  - pip install coverage
  - pip install coveralls
  - pip install nose
  - cd scripts
  - bash ./setup.sh -travisCI

script:
  - cd ../
  - nosetests --with-coverage --cover-erase --exclude-dir=/home/travis/virtualenv/python2.7.9/lib/python2.7/site-packages/*
  # - coverage combine
  # - coverage report --omit '/home/travis/virtualenv/python2.7.9/lib/python2.7/site-packages/*'
  - cd tests/
  - pwd
  - coverage run test_init.py

after_success:
  - cd ../
  - coveralls
